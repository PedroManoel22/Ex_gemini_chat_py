# Explique a diferença entre um iterável (como uma lista) e um iterador.

# iterável são todos os objetos que podem ser iteráveis, ou seja posso 'passar' por cada elemento da variavel (são os objetos que recebem o método __iter__()),
# já o iterador ele é o que executa esta iteração, passando por cada elemento (aqui implementamos o método __next__())