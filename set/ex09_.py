# Converta a seguinte lista [1, 2, 2, 3, 4, 4, 5]em um set para remover os elementos duplicados. Em seguida, 
# converta o set de volta para uma lista.

lista = [1, 2, 2, 3, 4, 4, 5]
lista = set(lista)
lista = list(lista)
print(lista)